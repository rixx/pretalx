---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: python
    tag: '3.7'

inputs:
 - name: pretalx

run:
  path: pretalx/ci/pretalx-package
  args: ["((twine_user))", "((twine_password))"]
