---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: python
    tag: '(( python_version ))'

inputs:
 - name: pretalx

run:
  path: pretalx/ci/pretalx-tests
  args: ["(( codecov ))"]
